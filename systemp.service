[Unit]
Description=System temperature Service

[Service]
User=pi
Group=pi
Type=simple
WorkingDirectory=/srv/systemp
ExecStart=bash /srv/systemp/run.sh
ExecStop=/bin/kill -2 $MAINPID
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
